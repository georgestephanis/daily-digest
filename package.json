{
  "name": "daily-digest",
  "version": "0.1.0",
  "description": "Daily Digest admin DataViews UI",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start",
    "lint": "composer lint && npm run lint:js",
    "lint:fix": "composer lint:fix && npm run lint:js:fix",
    "lint:js": "wp-scripts lint-js",
    "lint:js:fix": "wp-scripts lint-js --fix"
  },
  "devDependencies": {
    "@wordpress/prettier-config": "^4.17.0",
    "@wordpress/scripts": "^31.6.0"
  },
  "dependencies": {
    "@wordpress/components": "^32.3.0",
    "@wordpress/dataviews": "13.0.0",
    "@wordpress/dom-ready": "^4.41.0",
    "@wordpress/element": "^6.41.0",
    "@wordpress/i18n": "^6.14.0"
  }
}
